---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: billingplans.paas.kubeop.io
spec:
  group: paas.kubeop.io
  names:
    kind: BillingPlan
    listKind: BillingPlanList
    plural: billingplans
    singular: billingplan
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.currency
      name: CURRENCY
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BillingPlan defines metering and pricing configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BillingPlanSpec declares metering behaviour for tenants and
              projects.
            properties:
              currency:
                description: Currency specifies the ISO 4217 currency code used for
                  billing.
                pattern: ^[A-Z]{3}$
                type: string
              meters:
                description: Meters enables resource consumption tracking.
                properties:
                  cpu:
                    description: CPU enables metering for CPU usage.
                    type: boolean
                  egress:
                    description: Egress enables metering for network egress traffic.
                    type: boolean
                  ips:
                    description: IPs enables metering for static IPs.
                    type: boolean
                  lbHours:
                    description: LBHours enables metering for load balancer usage.
                    type: boolean
                  mem:
                    description: Memory enables metering for memory usage.
                    type: boolean
                  objects:
                    description: Objects enables metering for Kubernetes object counts.
                    type: boolean
                  storage:
                    description: Storage enables metering for persistent storage.
                    type: boolean
                type: object
              rates:
                description: Rates declares the currency rates for enabled meters.
                properties:
                  cpu:
                    description: CPURate identifies the price per CPU unit.
                    type: string
                  egress:
                    description: EgressRate identifies the price per GiB of egress
                      traffic.
                    type: string
                  ips:
                    description: IPsRate identifies the price per static IP.
                    type: string
                  lbHours:
                    description: LBHoursRate identifies the price per load balancer
                      hour.
                    type: string
                  mem:
                    description: MemoryRate identifies the price per GiB of memory.
                    type: string
                  objects:
                    description: ObjectsRate identifies the price per object.
                    type: string
                  storage:
                    description: StorageRate identifies the price per GiB of storage.
                    type: string
                type: object
            required:
            - currency
            type: object
          status:
            description: BillingPlanStatus tracks readiness information for a billing
              plan.
            properties:
              conditions:
                description: Conditions surfaces readiness and validation signals.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
