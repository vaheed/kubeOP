---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: configrefs.paas.kubeop.io
spec:
  group: paas.kubeop.io
  names:
    kind: ConfigRef
    listKind: ConfigRefList
    plural: configrefs
    singular: configref
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.data.type
      name: TYPE
      type: string
    - jsonPath: .spec.scope
      name: SCOPE
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConfigRef stores reusable configuration payloads.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConfigRefSpec declares configuration attachments for applications.
            properties:
              data:
                description: Data contains the configuration payload.
                properties:
                  configMapRef:
                    description: ConfigMapRef references an existing ConfigMap when
                      Type is configMap.
                    type: string
                  inline:
                    additionalProperties:
                      type: string
                    description: Inline stores literal key-value pairs when Type is
                      inline.
                    type: object
                  type:
                    description: Type identifies the source of the configuration data.
                    enum:
                    - inline
                    - configMap
                    type: string
                required:
                - type
                type: object
              mountPath:
                description: MountPath defines the path used when Scope is volume.
                type: string
              scope:
                default: volume
                description: Scope selects how the data is presented to workloads.
                enum:
                - volume
                - env
                type: string
            required:
            - data
            type: object
          status:
            description: ConfigRefStatus conveys readiness of the configuration projection.
            properties:
              conditions:
                description: Conditions summarise readiness for mounting or injecting
                  the configuration.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: metadata.labels.paas.kubeop.io/tenant is required
          rule: "'paas.kubeop.io/tenant' in self.metadata.labels"
        - message: metadata.labels.paas.kubeop.io/project is required
          rule: "'paas.kubeop.io/project' in self.metadata.labels"
        - message: metadata.labels.paas.kubeop.io/app is required
          rule: "'paas.kubeop.io/app' in self.metadata.labels"
    served: true
    storage: true
    subresources:
      status: {}
