---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: alertpolicies.paas.kubeop.io
spec:
  group: paas.kubeop.io
  names:
    kind: AlertPolicy
    listKind: AlertPolicyList
    plural: alertpolicies
    singular: alertpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertPolicy controls alert delivery for tenants, projects, or
          apps.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertPolicySpec defines routing behaviour for alerts emitted
              by kubeOP resources.
            properties:
              matchLabels:
                additionalProperties:
                  type: string
                description: MatchLabels selects subjects that should use this policy.
                type: object
              routes:
                description: Routes enumerates notification targets.
                items:
                  description: AlertRoute defines a notification target for alerts.
                  properties:
                    endpoint:
                      description: Endpoint is the URL or integration key for the
                        route.
                      minLength: 1
                      type: string
                    name:
                      description: Name identifies the route for reference by severity
                        rules.
                      minLength: 1
                      type: string
                    secretRef:
                      description: SecretRef optionally references sensitive credentials.
                      type: string
                    type:
                      description: Type declares the destination integration type.
                      enum:
                      - pagerduty
                      - webhook
                      - slack
                      type: string
                  required:
                  - endpoint
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
              severityRules:
                description: SeverityRules maps severities to routes.
                items:
                  description: AlertSeverityRule wires severities to routes.
                  properties:
                    routes:
                      description: Routes lists route names that should receive the
                        alert.
                      items:
                        type: string
                      minItems: 1
                      type: array
                    severity:
                      description: Severity identifies the severity level for the
                        rule.
                      enum:
                      - info
                      - warning
                      - critical
                      type: string
                  required:
                  - routes
                  - severity
                  type: object
                minItems: 1
                type: array
            required:
            - routes
            - severityRules
            type: object
          status:
            description: AlertPolicyStatus reports reconciliation results for an alert
              policy.
            properties:
              conditions:
                description: Conditions conveys readiness and configuration health.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
