{
  "name": "kubeop-docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:lint": "bash -lc 'set -euo pipefail; if ! command -v vale >/dev/null 2>&1; then go install github.com/errata-ai/vale/v2/cmd/vale@v2.27.0; fi; PATH=\"$(go env GOPATH)/bin:${PATH}\" vale --config=.vale.ini .'"
  },
  "devDependencies": {
    "vitepress": "^1.1.0"
  }
}
