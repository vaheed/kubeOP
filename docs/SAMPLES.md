Samples

- Example manifests and flavor catalog are included under `samples/`:
  - `samples/templates/manifests/example.yaml` — simple Deployment + Service
  - `samples/templates/helm/CHART_PLACEHOLDER.txt` — notes for using Helm tarballs
  - `samples/flavors/catalog.yaml` — example flavor definitions

Use these as starting points for templates or values in your deployments.

