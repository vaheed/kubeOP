# Copy to .env and fill with environment-specific values before running the sample.
# Values shown here are placeholders and should be replaced.
AUTH_TOKEN="replace-with-admin-jwt"
USER_EMAIL="alice@example.com"
CLUSTER_ID="clu-123"
PROJECT_ID="proj-123"
